# Functional symbols expansion · v0.6

User explicitly requests redesigning every formerly excluded Phosphor icon in Colorful's language. Scope is the 466 excluded entries in docs/catalog/phosphor-coverage.json, assigned disjointly in symbols-*.json. Existing 1053 assets stay intact.

Visual language: 24×24 viewBox, optically centered 3–21 working area, rounded actual geometry, three semantic theme tokens only: surface #f0e9f8, back #d9c9ed, detail #aa8bcf. Root detail stroke 1.1 with round caps/joins. Use open, confident linework, rounded capsules, generous negative space, meaningful local color accents. Do not force an illustration or arbitrary backplate onto a functional glyph. Circular/square containers only where semantically named. Preserve direction, counts, algebra meaning, recognizable identities. Brand silhouettes can be simplified, but not replaced with unrelated letters or generic shapes. Keep corresponding family proportions consistent; variants must remain visibly meaningful.

Author explicit paths/family rules (no automatic transformed vendor paths, no placeholder/fallback). Inspect vendor/phosphor/raw by name as semantic reference. core-kit.mjs helpers may be imported without modifying that shared file. Add your own namespaced helper if needed. Rounded stroke lines are important so thin/light/regular/bold differ; colored surfaces can support them. At least two semantic layers per design where appropriate; do not add meaningless layers solely to hit a quota.

Each group owns only design/redesign/symbols-GROUP.mjs, own helpers, assets/regular for assigned names, and design/redesign/notes/symbols-GROUP.json. Notes entries {name,family,visualReason}; optional label (Chinese), category, tags welcome for root integration. Generator must iterate explicit assignment and throw on unimplemented names. Write standalone SVG with xmlns, width/height 24, viewBox 0 0 24 24, fill none, detail root stroke. Use scripts/round-svg.mjs roundSVG on each output to round actual joins. No global generation/check commands until integration; root runs them. No catalog/integration/test changes by child agents. No commits.

Inspect generated contact sheets or representative raster previews with local sharp runtime if useful. Report count, family designs, any unresolved issue honestly. Root integrates shared catalog, source coverage, reviews, API and full verification.
