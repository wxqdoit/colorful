"use client";
/* GENERATED FILE — edit assets/regular or assets/catalog.json; run npm run assemble. */
import { forwardRef } from "react";
import type { Icon } from "../lib/types";
import IconBase from "../lib/IconBase";
import artwork from "../defs/ArrowElbowRightDown";

/** 先右再下直角转折箭头 · 方向与布局 */
const I: Icon = forwardRef((props, ref) => <IconBase ref={ref} {...props} weights={artwork} />);
I.displayName = "ArrowElbowRightDownIcon";
export { I as ArrowElbowRightDownIcon, I as ArrowElbowRightDown };
