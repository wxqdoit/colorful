"use client";
/* GENERATED FILE — edit assets/regular, assets/overrides or assets/catalog.json; run npm run assemble. */
import { forwardRef } from "react";
import type { Icon } from "../lib/types";
import IconBase from "../lib/IconBase";
import weights from "../defs/OriginalVirtualRealityHeadset";

/** 虚拟现实头显 · 科技 */
const I: Icon = forwardRef((props, ref) => <IconBase ref={ref} {...props} weights={weights} />);
I.displayName = "OriginalVirtualRealityHeadsetIcon";
export { I as OriginalVirtualRealityHeadsetIcon, I as OriginalVirtualRealityHeadset };
