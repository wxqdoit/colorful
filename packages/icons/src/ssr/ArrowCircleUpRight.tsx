/* GENERATED FILE — edit assets/regular or assets/catalog.json; run npm run assemble. */
import { forwardRef } from "react";
import type { Icon } from "../lib/types";
import SSRBase from "../lib/SSRBase";
import artwork from "../defs/ArrowCircleUpRight";

/** 圆框向右上箭头 · 方向与布局 */
const I: Icon = forwardRef((props, ref) => <SSRBase ref={ref} {...props} weights={artwork} />);
I.displayName = "ArrowCircleUpRightIcon";
export { I as ArrowCircleUpRightIcon, I as ArrowCircleUpRight };
