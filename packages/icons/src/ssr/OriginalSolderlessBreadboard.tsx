/* GENERATED FILE — edit assets/regular or assets/catalog.json; run npm run assemble. */
import { forwardRef } from "react";
import type { Icon } from "../lib/types";
import SSRBase from "../lib/SSRBase";
import artwork from "../defs/OriginalSolderlessBreadboard";

/** 免焊面包板 · 科技 */
const I: Icon = forwardRef((props, ref) => <SSRBase ref={ref} {...props} weights={artwork} />);
I.displayName = "OriginalSolderlessBreadboardIcon";
export { I as OriginalSolderlessBreadboardIcon, I as OriginalSolderlessBreadboard };
