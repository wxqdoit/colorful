/* GENERATED FILE — edit assets/regular, assets/overrides or assets/catalog.json; run npm run assemble. */
import { forwardRef } from "react";
import type { Icon } from "../lib/types";
import SSRBase from "../lib/SSRBase";
import weights from "../defs/OriginalTrackedInspectionRobot";

/** 履带巡检机器人 · 科技 */
const I: Icon = forwardRef((props, ref) => <SSRBase ref={ref} {...props} weights={weights} />);
I.displayName = "OriginalTrackedInspectionRobotIcon";
export { I as OriginalTrackedInspectionRobotIcon, I as OriginalTrackedInspectionRobot };
