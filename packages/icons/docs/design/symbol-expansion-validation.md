# v0.6.0 功能符号扩展验收

2026-09-11：补齐原目录排除的 466 个名称，总数 1,519，覆盖锁定的上游 1,512 个名称，项目专属 7 个。

- 四组设计源：方向与布局 141、文字与运算 141、品牌 79、功能符号 105。逐项路由、中文/品牌名称和设计说明与目录一致。
- 在整组接触表检查 24/26px 与放大稿；方向、数量、运算区域与品牌识别结构保留。修正三圆交集范围、间性符号和 HTML 标记。复杂品牌曲线为小尺寸简化重绘。
- `npm run check` 通过：资产同步、全量圆角、两套 TypeScript 检查、34 项测试、CSR/SSR 与 UMD 构建、包入口/类型/按需导入检查，以及预览构建。
- `node design/weights/check-bounds.mjs`：9,114 个样式资产，越界 0。圆角审计 9,114 个样式，剩余问题 0。
- 浏览器确认：新增方向分类、文字运算深色显示、彩色笔画分层、对照页 466 范围与 Phosphor 原版标注。开发服务器在批量新增文件后刷新了按需导入索引。
- 预览主入口仍有 Vite 大于 500 kB 的体积提示（约 606 kB）；单图标保持独立按需加载，包的单图标 tree shaking 检查通过。

生成包：`colorful-icons-react-0.6.0.tgz`。
